<web-app xmlns="http://java.sun.com/xml/ns/javaee" version="2.5">
    <listener>
        <listener-class>groovyx.gaelyk.GaelykServletContextListener</listener-class>
    </listener>

    <servlet>
        <servlet-name>GroovletServlet</servlet-name>
        <servlet-class>groovyx.gaelyk.GaelykServlet</servlet-class>
        <init-param>
            <param-name>verbose</param-name>
            <!-- Set it to true for more details -->
            <param-value>false</param-value>
        </init-param>
        <!-- usefule for warmup requests -->        
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet>
        <servlet-name>TemplateServlet</servlet-name>
        <servlet-class>groovyx.gaelyk.GaelykTemplateServlet</servlet-class>
        <init-param>
            <!-- Remove the default "generated by" messages from the templates -->
            <param-name>generated.by</param-name>
            <param-value>false</param-value>
        </init-param>
        <init-param>
            <param-name>verbose</param-name>
            <!-- Output generation time in the HTML, see source page -->
            <param-value>false</param-value>
        </init-param>
        <init-param>
            <param-name>preferPrecompiled</param-name>
            <param-value>false</param-value>
        </init-param>
        <!-- usefule for warmup requests -->        
        <load-on-startup>1</load-on-startup>
    </servlet>

    <filter>
        <filter-name>RoutesFilter</filter-name>
        <filter-class>groovyx.gaelyk.routes.RoutesFilter</filter-class>
    </filter>

    <servlet-mapping>
        <servlet-name>GroovletServlet</servlet-name>
        <url-pattern>*.groovy</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>TemplateServlet</servlet-name>
        <url-pattern>*.gtpl</url-pattern>
    </servlet-mapping>

    <filter-mapping>
        <filter-name>RoutesFilter</filter-name>
        <url-pattern>/*</url-pattern>
        <dispatcher>INCLUDE</dispatcher>
        <dispatcher>FORWARD</dispatcher>
        <dispatcher>REQUEST</dispatcher>
        <!--
            be sure to keep the ERROR dispatcher if you want to be able to define a location in your error pages
            that will redirect to a route defined in routes.groovy
         -->        
        <dispatcher>ERROR</dispatcher>
    </filter-mapping>

    <!-- Uncomment the following security constraint if you want to secure your incoming email endpoint -->
    <!-- Only allow the SDK and administrators to have access to the incoming email endpoint -->
    <security-constraint>
        <web-resource-collection>
            <url-pattern>/admin/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>admin</role-name>
        </auth-constraint>
    </security-constraint>
    <security-constraint>
        <web-resource-collection>
            <url-pattern>/_ah/mail/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- catch 404 not found errors -->
    <error-page>
        <error-code>404</error-code>
        <!--
            with the ERROR dispatcher in the filter,
            we can use a route defined in routes.groovy for the error handling
        -->
        <location>/not-found</location>
    </error-page>
    <error-page>
        <error-code>403</error-code>
        <location>/not-allowed</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/error</location>
    </error-page>
    <error-page>
        <exception>java.lang.Throwable</exception>
        <location>/error</location>
    </error-page>
    
    <!-- Uncommented to avoid the habit of the container to try /foo/index.gtpl after /foo was not found -->
    <!--
    <welcome-file-list>
        <welcome-file>index.gtpl</welcome-file>
    </welcome-file-list>
    -->
</web-app>
